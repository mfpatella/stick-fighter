# Local development works with these values blank.
# Add Supabase values when we connect a real project.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Used only by scripts or deployment tools, never by browser code.
SUPABASE_PROJECT_REF=
